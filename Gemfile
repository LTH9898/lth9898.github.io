source 'https://rubygems.org'

# Mirrors what GitHub Pages runs server-side, so a local build matches the
# deployed one. Install with: bundle install --path vendor/bundle
gem 'github-pages', group: :jekyll_plugins
